# توثيق عملية الدفع (كاش + فيزا)

## السيناريوهات المدعومة
- **دفع كامل كاش**: يتم تسجيل حركة واحدة `CASH`.
- **دفع كامل فيزا/كارت**: يتم تسجيل حركة واحدة `CARD`.
- **دفع مختلط (كاش + فيزا)**: يتم إرسال أكثر من حركة دفع في نفس الطلب، وكل حركة تُسجل بشكل مستقل (مثال: 50 كاش + 100 فيزا).

## قواعد التحقق
- إجمالي الحركات (`sum(payments.amount)`) لازم يكون أكبر من صفر.
- إجمالي الحركات لا يمكن يتجاوز المبلغ المتبقي على الفاتورة.
- عند نجاح العملية:
  - يتم تحديث `invoice.paid_amount` بمجموع الحركات.
  - يتم تحديث حالة الفاتورة تلقائيًا: `UNPAID` / `PARTIAL` / `PAID`.
  - يتم إنشاء حركة (Transaction) لكل جزء دفع لتتبع واضح.

## شكل الطلب (API)
`POST /api/v1/appointments/{appointment}/billing/payments`

```json
{
  "payments": [
    { "amount": 50, "method": "CASH" },
    { "amount": 100, "method": "CARD" }
  ]
}
```

> ما زال الشكل القديم مدعومًا للتوافق الخلفي:
> 
> ```json
> { "amount": 150, "method": "CASH" }
> ```

## ملاحظات تشغيلية
- في شاشة الاستقبال (Payment Modal) أصبح الإدخال واضحًا لقيمتين منفصلتين: كاش وكارت.
- الإيصال يعرض أن الدفع **مختلط** عند وجود القيمتين.
- التقارير أو أي شاشة تعتمد على `billing.transactions` ستقرأ الحركات الفعلية بدل افتراض حركة واحدة مجمعة.
